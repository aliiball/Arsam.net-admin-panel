# API base URL. Empty/relative uses MSW mocks in dev.
VITE_API_BASE_URL=http://localhost:8000/api

# Feature flags (optional)
VITE_ENABLE_AI_ASSISTANT=true

# Default layout mode when no per-user preference exists: sidebar | topnav
VITE_DEFAULT_LAYOUT_MODE=sidebar
