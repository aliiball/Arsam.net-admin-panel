# Task 002 — Primitives & Feedback

## Objective
Add the P0 primitive set (themed shadcn/ui) and feedback/state components with full Storybook coverage.

## Steps
1. Add primitives: Button, Input, Textarea, Select, Combobox, Checkbox, Radio, Switch, Slider, Badge, Avatar, Tooltip, Popover, Dialog, Sheet, DropdownMenu, Tabs, Accordion, Card, Separator, Skeleton, Breadcrumb, Pagination, ScrollArea.
2. Feedback: EmptyState, ErrorState, LoadingState/Spinner, InlineAlert, ConfirmDialog.
3. Ensure token-only styling + `cva` variants + `data-action`/`data-entity` where interactive.
4. Stories (default/loading/empty/error/mobile) + `play` tests; a11y clean.

## Acceptance criteria
- [ ] All listed components present, themed, token-only.
- [ ] Full story states + interaction tests; a11y clean; strict TS.
- [ ] Verify green; `dod-reviewer` PASS. STOP for user commit.
